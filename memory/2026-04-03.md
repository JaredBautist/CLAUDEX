# 2026-04-03

- Se diagnosticó `LLM request failed: network connection error.` como combinación de:
  - Ollama local caído (`127.0.0.1:11434` rechazando conexión).
  - Modelo default de OpenClaw en `ollama/qwen2.5-coder:3b`.
  - Proxy forzando `model: openclaw` en vez de respetar perfil/modelo.
- Se actualizó launcher y proxy para:
  - Resolver `provider/model` correctamente.
  - Validar salud de Ollama antes de arrancar (fail-fast, no timeout ambiguo).
  - Habilitar ruta directa `ollama/*` -> `OLLAMA_BASE_URL` sin depender de OpenClaw para ese salto.
- Se amplió soporte de modelos OpenAI (passthrough) en allowlist/parser para evitar bloqueo por hardcode de `gpt-5.4`.
- Se creó `.claudexrc.json` local con perfiles `openai`, `openai-mini`, `ollama`.
- Se ajustó `C:\Users\dylam\.openclaw\openclaw.json`:
  - `agents.defaults.model.primary = openai-codex/gpt-5.4`
  - `agents.defaults.timeoutSeconds = 180`
  - limpieza de plugin stale `google-gemini-cli-auth`
- Verificación: `bun run typecheck` y `bun run smoke:scripts` OK.
